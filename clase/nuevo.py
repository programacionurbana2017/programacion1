def heber(navegar):
	contactos=[]
	navegar= open ("archivo.txt","r")
	
	for k in navegar:
		registro=k.split(",")
		print (registro)
		
	
	

class contactos:
	def init (self,nombre):
		self.nombre
		self.apellido
		contactos=contactos()
	
a = []

llamar = contactos(heber(a))
	


file = open ("archivo.txt", "r")

for k in file:
		registro=k.split(",")
		print (registro)
		contactos.append(registro)
			
			
			
			
			contactos=[]

file = open ("archivo.txt", "r")

for k in file:
		registro=k.split(",")
		print (registro)
		contactos.append(registro)
			
			
heber(a)

busqueda={}
busqueda["nombre"]
busqueda.nombre(nombre)